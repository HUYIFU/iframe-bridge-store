{
  "name": "iframe-bridge-store",
  "version": "1.0.0-alpha.2",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "tsc",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "penpal": "^6.2.2",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
